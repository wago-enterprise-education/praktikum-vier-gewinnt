// Lösung zu den Aufgaben für das 4-Gewinnt Spiel

// Pins definieren
// Aufgabe 2
// #define LED 23

// Aufgabe 3
// #define Masse 22

// Aufgabe 4
// #define LED1 13
// #define LED2 22
// #define Masse1 12
// #define Masse2 23

void setup() {
  // Aufgabe 2
  // pinMode(LED, OUTPUT);
  
  // Aufgabe 3
  // pinMode(Masse, OUTPUT);

  // pinMode(LED1, OUTPUT);
  // pinMode(LED2, OUTPUT);
  // pinMode(Masse1, OUTPUT);
  // pinMode(Masse2, OUTPUT);

}

void loop() {
  // Aufgabe 2
  // digitalWrite(LED, HIGH);
  // delay(2000);
  // digitalWrite(LED, LOW);
  // delay(2000);

  // Aufgabe 3
  // digitalWrite(LED, HIGH);
  // digitalWrite(Masse, LOW);
  // delay(2000);
  // digitalWrite(Masse, HIGH);
  // delay(2000);

  // Aufgabe 4
  // 4.1
  // y Koordinate
  // digitalWrite(LED1, HIGH);
  // x Koordinate
  // digitalWrite(Masse1, LOW);
  // digitalWrite(LED2, LOW);
  // digitalWrite(Masse2, HIGH);
  
  // 4.2
  // Erste LED einschalten 
  // digitalWrite(LED1, HIGH);
  // digitalWrite(Masse1, LOW);
  // digitalWrite(LED2, LOW);
  // digitalWrite(Masse2, HIGH);
  // Verzögerung
  // delay(1);
  // Erste LED ausschalten
  // digitalWrite(LED1, LOW);
  // digitalWrite(Masse1, HIGH);
  //Zweite LED einschalten 
  // digitalWrite(LED2, HIGH);
  // digitalWrite(Masse2, LOW);
  // Verzögerung
  // delay(1);
  // zweite LED ausschalten
  // digitalWrite(LED2, LOW);
  // digitalWrite(Masse2, HIGH);
}
